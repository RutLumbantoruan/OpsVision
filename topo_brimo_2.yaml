anchorLinks:
  - link: &grafana-home
      url: "http://localhost:3000/"
      params: "from=now-1h&to=now"

cells:
  ocp:
    fillColor:
        gradientMode: "hue"
        thresholds:
          - {color: "semi-dark-green", level: 0}
          - {color: "orange", level: 400}
          - {color: "red", level: 800}
      link: *grafana-home
